# .gitignore

# Dependencies
# This directory contains all of the project's installed npm packages.
# It can be very large and should be generated by running `npm install`.
node_modules/

# --- Media Files ---
# This is the line that solves your problem. It tells Git to ignore
# the entire 'media' directory and everything inside it.
media/

# Environment Variables
# This file contains environment-specific configuration and potential secrets.
# It should never be committed to source control. A .env.example file is
# often created to show what variables are needed.
.env

# Logs
# Log files are generated during runtime and should not be versioned.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
# If you were using a build step (e.g., with TypeScript), you would ignore the output.
dist/
build/

# Optional - IDE and OS files
# These are configuration files for specific editors or operating systems.
# Ignoring them keeps the repository clean for all collaborators.
.vscode/
.idea/
.DS_Store
*.suo
*.user
*.userosscache
*.sln.docstates